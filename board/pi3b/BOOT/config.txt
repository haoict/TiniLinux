# Please note that this is only a sample, we recommend you to change it to fit
# your needs.
# You should override this file using BR2_PACKAGE_RPI_FIRMWARE_CONFIG_FILE.
# See http://buildroot.org/manual.html#rootfs-custom
# and http://elinux.org/RPiconfig for a description of config.txt syntax

### NOTE: when you change it, you should "make rpi-firmware-dirclean" to make update to sdcard.img

start_file=start.elf
fixup_file=fixup.dat

kernel=Image

# To use an external initramfs file
initramfs initramfs

# Disable overscan assuming the display supports displaying the full resolution
# If the text shown on the screen disappears off the edge, comment this out
disable_overscan=1

# How much memory in MB to assign to the GPU on Pi models having
# 256, 512 or 1024 MB total memory
gpu_mem_256=100
gpu_mem_512=200
gpu_mem_1024=300

# Enable KMS
dtoverlay=vc4-kms-v3d
dtoverlay=pi-st7789v-240x240

# if use legacy fbtft instead of our custom firmware pi-st7789v-240x240, uncomment these line and comment the above line
#dtoverlay=fbtft,spi0-0,st7789v,dc_pin=25,reset_pin=27,speed=64000000,rotate=270,bgr=1
#dtparam=width=240
#dtparam=height=240

# enable 64bits support
arm_64bit=1
